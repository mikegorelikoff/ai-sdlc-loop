# Write specification

## Entry

Preparation passed.

## Procedure

Use `ai-sdlc-test-cases` to make observable scenarios explicit when the change is non-trivial. Run `python3 <skills-root>/ai-sdlc-shared-runtime/scripts/loop.py specify ...`. Persist only `.toon` artifacts and show the returned fingerprint.

## Exit

Ask the user to approve or reject Implement for that exact fingerprint. Never record approval on the user's behalf.
