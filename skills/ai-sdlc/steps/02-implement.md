# Implement route

## Entry

A current specification exists and the user explicitly approved its fingerprint.

## Procedure

Load `ai-sdlc-implement`; it owns receipt validation, scope enforcement, and source mutation.

## Exit

Hand changed paths to `ai-sdlc-verify` without implying readiness.
