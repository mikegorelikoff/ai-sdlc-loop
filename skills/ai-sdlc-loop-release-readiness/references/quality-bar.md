# Release readiness quality bar

Bind evidence to one exact commit. Required gates include local validation, hosted supported-platform CI, approval state, artifact/version identity, and any manual acceptance explicitly required by the release. A release is `ready` only when every declared gate passed and no blocker remains. Keep residual risks visible even when they do not block release.
