# Implement bounded change

## Entry

Implement eligibility passed.

## Procedure

Use `ai-sdlc-loop-branching` when a task branch is required. Edit only paths listed in `spec.toon`. Preserve unrelated tracked, staged, unstaged, and untracked work. Do not commit.

## Exit

Summarize changed paths and transfer ownership to `ai-sdlc-loop-verify`.
