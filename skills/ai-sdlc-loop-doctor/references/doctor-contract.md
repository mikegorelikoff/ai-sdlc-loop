# Doctor Contract

Checks are deterministic observations with stable codes, evidence, and remediation. A failed check never implies that repair was attempted. The overall report is healthy only when every required check passes.

An upgrade plan compares safe relative skill paths and exact SHA-256 tree digests. It reports add, modify, remove, and unchanged actions plus a deterministic fingerprint. The plan is evidence for a later approved installation action; it has no apply authority.
